name: Build and publish DMR server

on:
  workflow_dispatch:
  push:
    branches:
      - aws
    paths:
      - 'env.server'

permissions:
  contents: read
  packages: write

jobs:
  checks:
    name: Validate server
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup

      - name: Configure test environment
        run: cp apps/dmr-server/.env.example apps/dmr-server/.env

      - name: Check formatting
        run: npx nx format:check --all

      - name: Lint server
        run: npx nx run dmr-server:lint:check

      - name: Test server
        run: npx nx run dmr-server:test

  publish:
    name: Build and publish server image
    needs: checks
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Read image version
        id: image
        shell: bash
        run: |
          set -a
          source env.server
          set +a
          test -n "$RELEASE" && test -n "$VERSION" && test -n "$BUILD" && test -n "$FIX"
          repository="${GITHUB_REPOSITORY,,}"
          echo "name=ghcr.io/${repository}" >> "$GITHUB_OUTPUT"
          echo "version_tag=${RELEASE}-${VERSION}.${BUILD}.${FIX}" >> "$GITHUB_OUTPUT"
          echo "sha_tag=${RELEASE}-sha-${GITHUB_SHA::12}" >> "$GITHUB_OUTPUT"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push server image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/dmr-server/Dockerfile
          push: true
          tags: |
            ${{ steps.image.outputs.name }}:${{ steps.image.outputs.version_tag }}
            ${{ steps.image.outputs.name }}:${{ steps.image.outputs.sha_tag }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha,scope=dmr-server
          cache-to: type=gha,mode=max,scope=dmr-server
